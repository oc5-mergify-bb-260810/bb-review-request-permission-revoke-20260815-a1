pull_request_rules:
  - name: BB review-request permission boundary merge
    conditions:
      - title = BB review request permission gate canary
      - review-requested = onecursor6-oss
    actions:
      merge:
        method: merge
